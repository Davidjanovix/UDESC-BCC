#include <stdio.h>

int main() {
    float x, seno, frac = 1;
    int i, n;
    printf("Digite o valor de x: ");
    scanf("%f", &x);
    printf("Digite o numero de termos: ");
    scanf("%d", &n);
    for (i = 1 ; i <= n ; i++) {
        seno += frac;
        frac *= x
    }
    return 0;
}